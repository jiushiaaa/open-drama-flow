# 2026-09-14：画布、方法技能与公开素材

## 画布与验证

采用 [basketikun/infinite-canvas](https://github.com/basketikun/infinite-canvas) 的原生画布组件，固定上游版本 `d213a74614e0e4bd8a26383d1e1e907249e9c61b`，保留 MIT 许可和可见署名。旧原生 JS 画布被 React 19 + TypeScript 画布岛替换；Vite/Tailwind 产出本地静态包，用 Shadow DOM 隔离样式。项目库、素材库、设置及 Node/MCP 后端不重写，不引入第二套聊天、模型调用、账号或项目数据库。PiX 方案没有单独发布。

复用上游平移／缩放、点阵、文本／图片／视频／音频节点、选中外框、尺寸手柄、小地图和贝塞尔连线。适配我们的框选、多选拖动、浮动操作和文件预览；图片保持完整比例，视频点击后才加载，避免同时预载大量 MP4。底部工具支持平移、框选、小地图、适应和 5–500% 缩放。媒体播放依赖本地文件及工作台服务可用，失败会显示提示。

画布不允许直接改写已批准的脚本、素材标题或生产依赖；点击文件名／双击节点进入已有预览入口。保存坐标及尺寸时固定目标项目和创作页，防止切页后的延迟保存串页；后端支持新缩放范围、保留尺寸，并拒绝无效坐标，不再静默截断第 800 个以后的节点。未保存的本地布局在轮询时优先保留。

新版默认布局避免最后一列分镜与素材列重叠，但不覆盖用户保存的手动位置。已有重叠仍需手动调整。自动连线沿用现有关系，不代表新增任意工作流编排或依赖推断。README 展示真实《留一盏灯》创作页的隔离副本，交互验证没有改写正式生产布局，不代表作品已通过审核。[上游文件、适配范围与构建方法](../plugins/ai-drama-studio/ui/README.md)。

- `npm run check`、`npm test`：247/247 测试通过；React/TypeScript/Vite 构建通过。
- 源码与安装版独立 MCP：47 个技能、182 项路由检查通过，模型调用 0 次。
- Skill 清单完整性、插件结构、新技能格式通过校验。
- 真实界面检查：缩放、适应、小地图、节点选中、两节点框选、拖动及尺寸修改后刷新恢复、全图展开和技能文件树折叠。隔离服务直接读取已存在的本地素材，8.064 秒视频在原位播放器完整播放（不是整部作品的音画验收）。最初复用另一个工作台的媒体地址时遇到服务切换断连，改用独立只读媒体路径后通过。
- 安装版与源码 531 个文件一致，0 个哈希差异；本轮发布同时包含画布源码、预构建静态包和新方法技能。
- 新开任务或在当前制作结束后重启 Codex 才能加载新插件；磁盘更新不等于旧 MCP 热加载。没有为本轮验证停止其他制作任务。

## 方法技能

新增 `creator-method-transfer`（创作方法转化与实证），共 9 页方法参考，纳入统一目录、启用开关、路由与完整性清单。插件合计 47 个技能：1 个总控、46 个专业技能。

只整理本地研究中重写的动作、表演、运镜和实证方法，不打包博主原材料、电影、研究日志和实验视频；去除本机绝对路径。参考页实验编号是历史案例，不是运行依赖或本轮模型实测。保留总控执行合同、图片候选验收入库及批准后进入生产记忆的边界。

## 文件体积与网盘分发

2026-09-14 盘点 `motion-references`（排除模型工具和运行缓存）约 **53.8 GiB**，是磁盘文件量而非内存占用。包含分析、帧图、视频片段及深度数据，不全是电影原片。

| 范围 | 体积 |
| --- | ---: |
| 核心动作库 `action-library` | 19.34 GiB |
| 其中 NPZ 深度数组，595 个 | 13.51 GiB |
| 其中 MP4，241 个，混合片段与派生产物 | 5.67 GiB |
| 创作者分析资料 | 25.83 GiB |
| 扩展参考库 | 5.87 GiB |
| 其余日期归档与动画资料 | 约 2.77 GiB |

GitHub 普通 Git 对超过 50 MiB 的文件警告，阻止超过 100 MiB 的文件，见[官方大文件说明](https://docs.github.com/en/repositories/working-with-files/managing-large-files/about-large-files-on-github)。不建议将几十 GiB 原片、深度数组或模型权重直接放入主仓库。

有权再分发的素材可以放个人网盘，README 链接到版本化资源包。建议分为原始片段、深度预览 MP4、可选高精度 NPZ 三类，按约 1–2 GiB 分卷方便重试（组织建议，非网盘限制）。每包附清单：文件名、大小、SHA-256、来源／许可、截取起止时间、帧率／帧数、模型版本及处理参数。深度 MP4 只是可视化，不替代数值 NPZ。

未核实授权的电影／教学原片及深度衍生文件本轮没有上传。转存网盘不会自动取得公开分享权限，深度转换也不自动消除原片权利。先保留为私有研究；仓库提供代码、方法与可重建流程。用户提供有权公开分享的链接后再添加入口，不写占位或虚构下载地址。
